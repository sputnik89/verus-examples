## Context
Read `mod.rs` about a trait called Database. There're some operations defined for that trait, each
of which is accompanied by a formal specification written in Verus.

## Problem
Based on for specifications, implement all the methods and their accompanying proofs so that the 
given specs are satisfied.

## Guidelines
- Do NOT change the trait and the specifications AT ALL!
- ONLY refer to `PROMPT.md` and `mod.rs` under `db/` for implementing the database! 
- Implement functions specified in mod.rs and helpers that are related to those functions ONLY!